<%=
  hash = { 
    channel: @channel,
    messages: @messages
  }
  if @channel.table.present?
    hash[:table] = {
      name: @channel.table.name,
      items: @channel.table.items,
      latest_moving_item: @latest_moving_item
    }
  end
  raw hash.to_json
%>
